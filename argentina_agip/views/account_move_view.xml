<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="priority">30</field>
        <field name="arch" type="xml">
                <button position="before">
                    <!-- <button name="compute_withholdings" string="Compute Withholdings" type="object" attrs="{'invisible': ['|', ('partner_type', '!=', 'supplier'), ('state', 'not in', ['draft', 'confirmed'])]}"/> -->
                    <button name="compute_partner_withholdings" string="Calcular Percepciones" type="object" />                    
                </button>
        </field>
    </record>

</odoo>
